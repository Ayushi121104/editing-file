#include <iostream>
using namespace std;

int main() {
  int num;

  cout << "enter value ";
  cin >> num;

  if ( num % 2 == 0)
    cout << num << " num is even.";
  else
    cout << num << "num is odd.";

  return 0;
}

