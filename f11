#include <iostream>
using namespace std;

int main() {
  int num;

  cout << "Enter an number: ";
  cin >> num;

  if ( num % 2 == 0)
    cout << num << " it is even.";
  else
    cout << num << "it is odd.";

  return 0;
}

